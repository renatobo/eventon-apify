## New Features

- None.

## Improvements

- None.

## Bug Fixes

- Corrected the bundled OpenAPI spec version: `info.version` and the manifest `provider_version` example still read `2.1.1` in the 2.2.0 build and now match the plugin version. The release script also bumps and verifies the OpenAPI version so it can no longer drift.
